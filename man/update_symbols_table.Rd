% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_tables.R
\name{update_symbols_table}
\alias{update_symbols_table}
\title{Update the table with all symbols basic information}
\usage{
update_symbols_table(indexes = NULL, db_con, save_data = TRUE)
}
\arguments{
\item{indexes}{A character vector specifying which indexes to filter by. Options include "ASX", "B3", "SP500", and "Benchmark". If NULL, returns all symbols.}

\item{db_con}{A DBI database connection object.}
}
\value{
Nothing. The function updates the 'all_symbols' table in the database, informing the user of the update status.
}
\description{
This function updates the 'all_symbols' table in the database with the latest stock symbols and their basic information from various indexes.
}
\details{
It fetches data from the ASX, B3, and S&P 500 indexes, combines them, and updates the database table accordingly.
}
\examples{
\dontrun{
# define the path and name of the database file
db_name <- "test_stock_db"
db_path <- file.path(tempdir(), db_name)
# create the database connection
db_con <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = FALSE)
# Update the symbols table
update_symbols_table(indexes = c("ASX", "B3"), db_con = db_con)
DBI::dbDisconnect(db_con)
}
}
